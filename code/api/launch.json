{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "node",
      "request": "attach",
      "name": "Attach to Docker",
      "address": "localhost",
      "port": 9229,
      "localRoot": "${workspaceFolder}/code/api",
      "remoteRoot": "/app" // ajuste conforme o caminho do código dentro do container
    }
  ]
}